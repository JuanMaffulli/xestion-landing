---
import { TESTIMONIOS } from '@data/config';
---

<section id="testimonios" class="py-20 sm:py-28 bg-slate-50 overflow-hidden">
  <div class="max-w-6xl mx-auto px-4 sm:px-6">

    <div class="text-center mb-12">
      <h2 class="reveal text-3xl sm:text-4xl font-bold text-slate-900 mb-4">
        Lo que dicen nuestros clientes
      </h2>
      <p class="reveal section-subheading !mb-0">
        Empresas que ya ordenaron su gestión con Xestion y mejoraron su día a día.
      </p>
    </div>
  </div>

  <!-- Carrusel horizontal -->
  <div class="reveal relative">
    <button
      id="test-prev"
      class="absolute left-2 sm:left-6 top-1/2 -translate-y-1/2 z-10 w-10 h-10 bg-white/90 backdrop-blur-sm rounded-full shadow-lg flex items-center justify-center hover:bg-white transition-colors"
      aria-label="Anterior"
    >
      <svg class="w-5 h-5 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
      </svg>
    </button>

    <div
      id="test-scroll"
      class="flex gap-6 overflow-x-auto px-4 sm:px-10 pb-4 snap-x snap-mandatory"
      style="scrollbar-width: none; -ms-overflow-style: none;"
    >
      {TESTIMONIOS.map((t) => (
        <div class="shrink-0 w-80 sm:w-96 snap-center bg-white rounded-2xl border border-slate-100 shadow-sm p-6 hover:shadow-lg hover:border-slate-200 transition-all duration-300">
          {/* Header */}
          <div class="flex items-center gap-4 mb-4">
            <div class="w-12 h-12 rounded-full overflow-hidden bg-slate-100 shrink-0">
              <img
                src={t.imagen}
                alt={t.empresa}
                class="w-full h-full object-cover"
                loading="lazy"
              />
            </div>
            <div>
              <h4 class="font-bold text-slate-800 text-sm">{t.empresa}</h4>
              <p class="text-xs text-slate-400">{t.subtitulo}</p>
            </div>
          </div>

          {/* Stars */}
          <div class="flex gap-0.5 mb-3">
            {[...Array(5)].map(() => (
              <svg class="w-4 h-4 text-yellow-400 fill-yellow-400" viewBox="0 0 24 24">
                <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"></path>
              </svg>
            ))}
          </div>

          {/* Text */}
          <p class="text-sm text-slate-600 leading-relaxed italic">
            "{t.texto}"
          </p>
        </div>
      ))}
    </div>

    <button
      id="test-next"
      class="absolute right-2 sm:right-6 top-1/2 -translate-y-1/2 z-10 w-10 h-10 bg-white/90 backdrop-blur-sm rounded-full shadow-lg flex items-center justify-center hover:bg-white transition-colors"
      aria-label="Siguiente"
    >
      <svg class="w-5 h-5 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
      </svg>
    </button>

    <p class="text-center text-sm text-slate-400 mt-4">
      Deslizá para ver más testimonios
    </p>
  </div>
</section>

<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const scroll = document.getElementById('test-scroll');
    const prev = document.getElementById('test-prev');
    const next = document.getElementById('test-next');
    const amount = 420;

    prev?.addEventListener('click', () => scroll?.scrollBy({ left: -amount, behavior: 'smooth' }));
    next?.addEventListener('click', () => scroll?.scrollBy({ left: amount, behavior: 'smooth' }));
  });
</script>

<style>
  #test-scroll::-webkit-scrollbar {
    display: none;
  }
</style>
